# PDF Assets

This page currently avoids publishing PDFs. Keep this directory empty unless the release plan changes.

If PDFs are added later, clear metadata and verify the files contain no author names, affiliations, acknowledgments,
project IDs, or non-anonymous URLs.
